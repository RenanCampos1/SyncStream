/**
 * Download URLs for the LAButuca desktop installer.
 *
 * Fill these in once the installer built with `pnpm run desktop:dist`
 * (output in `release/`) is hosted somewhere public, e.g.:
 *   "https://labutuca.app/download/LAButuca-Setup-1.0.0.exe"
 */
export const DESKTOP_WINDOWS_URL = "";
export const DESKTOP_MAC_URL = "";
export const DESKTOP_LINUX_URL = "";
